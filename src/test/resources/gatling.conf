#########################
# Gatling Configuration #
#########################

gatling {

  http {
    ahc {
      connectTimeout = 6000000                     # Timeout when establishing a connection
      readTimeout    = 6000000                     # Timeout when a used connection stays idle
      requestTimeout = 6000000                     # Timeout of the requests
    }
  }
}
